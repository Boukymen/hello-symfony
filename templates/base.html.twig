<!DOCTYPE html>
<html lang="{{app.request.locale}}">
	<head>
		<meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-tofit=no">
		<title>
			{% block title %}Hello city !
			{% endblock %}
		</title>
        {% block stylesheets %}
		<link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon" />
		<link href="{{ asset('/css/styles.css') }}" type="text/css" rel="stylesheet" />
        <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
        {% endblock %}
		{# Run `composer require symfony/webpack-encore-bundle`
				           and uncomment the following Encore helpers to start using Symfony UX #}
		{% block javascripts %}
			{#{{ encore_entry_script_tags('app') }}#}
		{% endblock %}
	</head>
	<body class="flex flex-col justify-between items-center min-h-screen">

        <main class="my-6 justify-center flex flex-col items-center" >
		{% block body %}{% endblock %}
        </main>
		<footer class="my-4">
			<p class="text-gray-400">
				&copy; Copyright
				{{ 'now'|date('Y')}}
				&middot;
				<a href="/about-us" class="text-indigo-500 hover:text-indigo-600 underline align-bottom">About Us</a>
			</p>
		</footer>
	</body>
</html>
